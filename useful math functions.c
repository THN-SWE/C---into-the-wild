#include <stdio.h>
#include <math.h> //including math header file to access math functions

int main(){
    /*
    sqrt();
    pow();
    ceil();
    floor();
    round();
    abs();
    fabs();
    log();
    sin();
    cos();
    tan();

    */
    double a;
    a = sqrt(9);

    double b;
    b = pow(2,3);

    double c;
    c = fabs(-3);

    printf("%lf\n",a);
    printf("%lf\n",b);
    printf("%lf\n",c);
    
    return 0;
}