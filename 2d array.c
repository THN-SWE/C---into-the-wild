#include <stdio.h>

int main()
{
    // 2d array where each element is an entire array
    // useful if you need a matrix, grid or table of data.

    // 2d array with 5 elements max ,each consists of an array of 3 elements
    int numbers[5][3] = {{1, 2, 3} {4, 5, 6}};

    
    return 0;
}